


![css containers](img/describe.png)

<step name="initial">When the app is opened or refreshed, only the [top slider] is active, with the teacher able to swipe left/right, choosing @student/@class.

![initial state](img/1_initial.png)

</step>

<hr>

<step name="student"> If the teacher selects @student from the [top slider], then the [ID dropdown menu] will activate, and the teacher can select a unique ID.

![student](img/2a_slide_student.png)

<example> if the teacher selects @student and then selects '148' from the [ID dropdown menu], this is what the GUI should look like:

![student 158](img/3_student_148.png)

</example>
</step>

<hr>

<step name="class">If the teacher selects @student from the [top slider], then the [second slider] will activate, with the teacher able to swipe left/right, choosing @pair/@cohort.

![class](img/2b_slide_class.png)

</step>

<hr>

<step name="pair">If the teacher selects @pair from the [second slider], then the [cohort dropdown menu] will activate, and the teacher can select a cohort.

![cohort](img/4a_slide_pair.png)

<example>if the teacher selects @pair, and then selects 'GCP-20' from the [cohort dropdown menu], this is what the GUI should look like:

![class GCP-20](img/5_select_class_gcp-20.png)

</example>
</step>

<hr>

<step name="reteach">If the teacher selects @reteach from the [second slider], then the [cohort dropdown menu] will activate, and the teacher can select a cohort. 

![reteach](img/4b_slide_reteach.png)

<example>if the teacher selects @reteach, and then selects 'BCP-180' from the [cohort dropdown menu], this is what the GUI should look like:

![BCP-180](img/6_select_class_bcp-180.png)

</example>
</step>
